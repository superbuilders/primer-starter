# Copy this file to game/.env for standalone game dev/builds.
#   cp game/.env.example game/.env

# Standalone bundles read Primer config directly from game/.env.
# Embedded bundles hydrate from host:init messages instead.

VITE_IS_EMBEDDED=false
VITE_PRIMER_PUBLISHABLE_KEY=pk_replace_me
